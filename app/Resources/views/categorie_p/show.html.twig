{% extends 'base.html.twig' %}

{% block body %}
    <h1>Categorie_p</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_P.id }}</td>
            </tr>
            <tr>
                <th>Nomc</th>
                <td>{{ categorie_P.nomc }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('categoriep_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('categoriep_edit', { 'id': categorie_P.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
